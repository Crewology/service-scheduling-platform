import { Link, useRoute } from "wouter";
import { Activity, ArrowLeft, ArrowRight, CalendarDays, DollarSign, LockKeyhole, UserRound } from "lucide-react";
import { MobileRoleViewToggle } from "@/components/shared/MobileRoleViewToggle";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { trpc } from "@/lib/trpc";
import { ProviderCustomersNav, formatDate, formatMoney, relativeAge, stageLabels } from "./ProviderCustomers";

export default function ProviderCustomerDetail() {
  const [, params] = useRoute("/provider/customers/:contactId");
  const contactId = Number(params?.contactId || 0);
  const access = trpc.customers.getAccess.useQuery();
  const detail = trpc.customers.getContact.useQuery({ contactId, eventLimit: 40 }, { enabled: access.data?.visible === true && contactId > 0, retry: false });

  if (access.isLoading || detail.isLoading) return <div className="container max-w-7xl animate-pulse py-8"><div className="h-48 rounded-3xl bg-slate-200" /><div className="mt-5 h-96 rounded-3xl bg-slate-100" /></div>;
  if (!access.data?.visible) return <div className="container max-w-2xl py-12"><Card><CardContent className="p-8 text-center"><LockKeyhole className="mx-auto h-9 w-9 text-slate-400" /><h1 className="mt-4 text-2xl font-bold">Customers is not available for this account</h1><Button asChild className="mt-5"><Link href="/">Return home</Link></Button></CardContent></Card></div>;
  if (!detail.data || detail.error) return <div className="container max-w-2xl py-12"><Card><CardContent className="p-8 text-center"><UserRound className="mx-auto h-9 w-9 text-slate-400" /><h1 className="mt-4 text-2xl font-bold">Relationship not found</h1><p className="mt-2 text-sm text-slate-600">This relationship may not belong to your provider account.</p><Button asChild className="mt-5"><Link href="/provider/customers">Back to Customers</Link></Button></CardContent></Card></div>;

  const { contact, events } = detail.data;
  return <div className="container max-w-7xl py-5 pb-28 sm:py-8 lg:pb-10"><MobileRoleViewToggle active="provider" /><div className="grid gap-6 lg:grid-cols-[232px_minmax(0,1fr)]"><ProviderCustomersNav active="detail" businessName={access.data.businessName} /><main className="min-w-0"><Link href="/provider/customers" className="inline-flex items-center gap-2 text-sm font-semibold text-[#174a73] hover:underline"><ArrowLeft className="h-4 w-4" />Customers</Link><section className="mt-4 rounded-[28px] bg-[#123f63] px-5 py-6 text-white shadow-[0_24px_70px_-38px_rgba(18,63,99,0.8)] sm:px-8 sm:py-8"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><Badge className="border-white/20 bg-white/10 text-blue-50 hover:bg-white/10">Read-only pilot</Badge><h1 className="mt-3 text-3xl font-bold tracking-tight">{contact.customerName || "Customer"}</h1><p className="mt-1 text-sm text-blue-100">{stageLabels[contact.effectiveStage] || contact.effectiveStage} · Customer since {formatDate(contact.firstInteractionAt)}</p><p className="mt-1 text-sm text-blue-100">{contact.customerEmail || "Email unavailable"}</p></div><div className="rounded-2xl border border-white/15 bg-white/10 px-4 py-3 text-sm text-blue-50"><LockKeyhole className="mr-2 inline h-4 w-4" />Source history only; no actions are sent.</div></div></section><section className="mt-5 grid gap-3 sm:grid-cols-3"><Stat icon={CalendarDays} label="Completed bookings" value={String(contact.completedBookingCount)} /><Stat icon={DollarSign} label="Captured value" value={formatMoney(contact.capturedValueCents)} /><Stat icon={CalendarDays} label="Next booking" value={contact.nextBookingAt ? formatDate(contact.nextBookingAt) : "None"} /></section><section className="mt-6 rounded-3xl border border-slate-200 bg-white p-5 sm:p-6"><div><p className="text-xs font-semibold uppercase tracking-[0.16em] text-blue-700">Relationship history</p><h2 className="mt-1 text-xl font-bold text-slate-950">Activity</h2><p className="mt-1 text-sm text-slate-500">This timeline is derived from authoritative OlogyCrew records. Private message text is not copied here.</p></div>{events.length ? <div className="mt-5">{events.map((event) => { const row = <div className="grid grid-cols-[36px_minmax(0,1fr)_auto] gap-3 border-b border-slate-100 py-4 last:border-0"><span className="flex h-9 w-9 items-center justify-center rounded-full bg-blue-50 text-[#174a73]"><Activity className="h-4 w-4" /></span><div><p className="font-medium text-slate-900">{event.summary}</p><p className="mt-0.5 text-xs text-slate-500">{event.eventType.replaceAll(".", " ")}</p></div><div className="flex items-center gap-2"><span className="text-xs text-slate-500">{relativeAge(event.occurredAt)}</span>{event.sourceHref ? <ArrowRight className="h-4 w-4 text-slate-400" /> : null}</div></div>; return event.sourceHref ? <Link key={event.id} href={event.sourceHref} className="block rounded-xl px-2 hover:bg-slate-50">{row}</Link> : <div key={event.id}>{row}</div>; })}</div> : <p className="mt-6 rounded-2xl border border-dashed p-6 text-center text-sm text-slate-500">No relationship activity has been projected yet.</p>}</section></main></div></div>;
}

function Stat({ icon: Icon, label, value }: { icon: typeof CalendarDays; label: string; value: string }) {
  return <Card><CardContent className="p-4"><div className="flex items-center gap-2 text-slate-500"><Icon className="h-4 w-4" /><span className="text-xs font-semibold uppercase tracking-[0.1em]">{label}</span></div><p className="mt-3 text-2xl font-bold text-slate-950">{value}</p></CardContent></Card>;
}
